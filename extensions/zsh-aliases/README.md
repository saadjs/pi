# @saadjs/pi-zsh-aliases

Use zsh aliases and functions in pi's `!` shell commands.

## Install

```bash
pi install npm:@saadjs/pi-zsh-aliases
```

For local development:

```bash
pi -e ./extensions/zsh-aliases/index.ts
```
